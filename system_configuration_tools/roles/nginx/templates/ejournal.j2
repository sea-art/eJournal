server {
    listen      80;
    server_name {{ inventory_hostname }};

    location / {
        include proxy_params;
        proxy_pass http://unix:{{ virtualenv_path }}/run/gunicorn.stock;
    }
}
