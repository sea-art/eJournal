language: python
python:
  - "3.6"
sudo: enabled
env:
  CODECOV_TOKEN="8904c3db-128a-4d3d-a2ff-d6a922227e4c"
before_install:
  - npm install -g npm@latest
install:
  - make setup-no-input
  - pip install codecov
script: make test
after_success:
  - codecov
branches:
  only:
    - master
    - develop
